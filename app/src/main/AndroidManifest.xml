<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rushil.zapsure">

    <uses-permission android:name="android.permission.INTERNET" />
    "
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Register_car"
            android:label="@string/title_activity_register_car"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Car_profile"
            android:label="@string/title_activity_car_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Upload_Pic_Car"
            android:label="@string/title_activity_upload__pic__car"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Claim_upload"
            android:label="@string/title_activity_claim_upload"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Result"
            android:label="@string/title_activity_result"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>